import eslintBaseConfig = require('./base');
import eslintTSBaseConfig = require('./ts-base');
import eslintWebConfig = require('./web');
import eslintNativeConfig = require('./native');

export {
  eslintBaseConfig,
  eslintTSBaseConfig,
  eslintWebConfig,
  eslintNativeConfig,
};
